#? this is a copy of the file params.yaml, made to avoid pushing secret_key and id to git
#? the real_params.yaml file is put in git ignore so it doesn't get pushed.
#? copy it to create the real params.yaml file and add you aws secret_id and key for
#? for the code to function.

preprocess:
  input: data/raw/diabetes.csv
  output: s3://lab1-s3-yana/data/processed/data.csv

train : 
  data : s3://lab1-s3-yana/data/processed/data.csv
  model_path : models/model.pkl
  random_state : 42
  n_estimators: 100
  max_depth: 5

aws :
  aws_access_key_id: ***
  aws_secret_access_key: ***
  region_name: eu-west-3

mlflow:
  MLFLOW_TRACKING_URI: http://13.36.243.42:5000/
